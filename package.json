{
  "scripts": {
    "dev": "parcel --open",
    "build": "parcel build index.html --no-source-maps -d docs --public-url /webcam-particle/ --out-file index"
  },
  "devDependencies": {
    "glslify-bundle": "^5.0.0",
    "glslify-deps": "^1.3.0"
  }
}
