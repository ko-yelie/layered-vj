{
  "scripts": {
    "dev": "parcel --open",
    "build": "parcel build index.pug --no-source-maps -d docs --public-url /webcam-particle/"
  },
  "devDependencies": {
    "glslify-bundle": "^5.0.0",
    "glslify-deps": "^1.3.0",
    "parcel-bundler": "^1.7.0",
    "pug": "^2.0.3",
    "stylus": "^0.54.5"
  }
}
